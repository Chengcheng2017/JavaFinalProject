<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chengcheng.leaguage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Homep.HomePage"
            android:label="Leaguage"
            >
        </activity>
        <activity android:name=".LogIn"
            android:label="Log In"/>

        <activity android:name=".Welcome" />
        <activity android:name=".Profile"
            android:label="Profile"/>
        <activity android:name=".SignUp"
            android:label="Sign Up"/>
        <activity android:name=".Recommendation"
            android:label="Recommendation"/>
        <activity android:name=".LearningP.LearningPath"
            android:label="Learning Path"/>
        <activity android:name=".Friends"
            android:label="Friends"/>
        <activity android:name=".CourseDe.CourseDetails"
            android:label="Course Details"/>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>